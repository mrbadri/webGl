{
  "name": "01",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "cd $npm_config_projectname && npm start",
    "build": "cd $npm_config_projectname && npm run build",
    "dev": "cd $npm_config_projectname && npm run dev"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "vite": "^5.2.10"
  },
  "dependencies": {
    "three": "^0.164.1"
  },
  "workspaces": [
    "packages/**/*"
  ]
}
